# Control Implementation Evidence

Index of *where* proof lives for each applicable Annex A control — not the evidence itself (that belongs in `14. Evidence`). Think of this as the map; `14. Evidence` is the territory.

Recommended format: a single evidence-mapping table (Control ID → Evidence type → File path → Date collected → Reviewer).
